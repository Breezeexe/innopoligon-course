# Практика 35 — Атаки на базы данных

- UUID: `50454e54-4553-542d-5033-350000000000`
- Тип: `PRACTICE`
- Проходной результат: 80%
- Максимум попыток: 4
- Баллы: 5

## Вопросы

### 1. Какой флаг извлечён инъекцией с объединением запросов?

Тип ответа: `INPUT_STRING`

### 2. Сколько столбцов возвращает уязвимый запрос product.php?

Тип ответа: `INPUT_STRING`

### 3. В какой таблице лежал флаг, добытый инъекцией через UNION?

Тип ответа: `INPUT_STRING`

### 4. Какой флаг извлечён из secret2 инъекцией второго порядка?

Тип ответа: `INPUT_STRING`

### 5. Какой флаг прочитан инъекцией из файла, закрытого правами служебной учётной записи СУБД?

Тип ответа: `INPUT_STRING`
